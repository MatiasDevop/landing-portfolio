/**
 * Shared props for icon components. All icons accept optional className
 * and forward standard SVG attributes for accessibility and styling.
 */
export interface IconProps {
	className?: string
	'aria-hidden'?: boolean
}
